<Project Sdk="Microsoft.NET.Sdk">

  <!-- Concrete implementations of the application abstractions: EF Core +
       PostgreSQL persistence (SangamDbContext, migrations), OpenIddict stores,
       Argon2id password hashing, audit writer, email/SMS senders. -->

  <PropertyGroup>
    <RootNamespace>Sangam.Identity.Infrastructure</RootNamespace>
    <Description>Sangam identity platform — infrastructure (persistence and external services).</Description>
  </PropertyGroup>

  <ItemGroup>
    <!-- Identity's token providers and OpenIddict's EF integration live in the shared framework. -->
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="EFCore.NamingConventions" />
    <PackageReference Include="Konscious.Security.Cryptography.Argon2" />
    <PackageReference Include="Microsoft.AspNetCore.Identity.EntityFrameworkCore" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" />
    <PackageReference Include="OpenIddict.EntityFrameworkCore" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Sangam.Identity.Application\Sangam.Identity.Application.csproj" />
    <ProjectReference Include="..\Sangam.Identity.Domain\Sangam.Identity.Domain.csproj" />
    <ProjectReference Include="..\Sangam.Shared\Sangam.Shared.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Sangam.Identity.Infrastructure.Tests" />
  </ItemGroup>

</Project>
